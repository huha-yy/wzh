大家开题报告今天算告一段落，从现在开始到12.31日是系统完成的时间，大家抓紧这段时间完成系统，注意以下要点：
1. 毕设要求系统需要10个表，大家建表的时候要符合mysql规范，可以先把表创建好发我过一遍。
2. 毕业设计重点是后面的写论文，所以我们对系统要求相对来说不是一定要完全没有bug，后面答辩时系统是录屏演示，只要在录屏的时候没有bug即可
3. 系统功能，如登录注册角色权限管理这些功能不是重点，后面论文的时候这些功能都是一笔带过，重点是和项目本身相关的核心功能
4. 系统可以使用诸如若依等框架生成框架代码

建表规范，我列举几个mysql建表规范点：
1. 所有表名、字段小写，不同字段下划线分隔
2. 所有表都要有id自增默认主键
3. 每个表最好都默认加上4个字段，创建者id、更新者id、创建时间、更新时间
4. varchar字段长度不要都是统一用默认的255，根据实际情况取2的n次方长度
5. 每个字段都有合理的注释
6. 表之间关联关系要正确，以学生、班级两个表为例，学生表不要建班级名、辅导员等字段，只需要班级id，然后通过班级id关联班级班找到班级信息